name: "Test mutex lock action"

on:
  pull_request:
    types: [opened, synchronize]

jobs:
  test01:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Run tests
        run: |
          npm install
          npm test
