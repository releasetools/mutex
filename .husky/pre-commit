# Format staged files
prettier $(git diff --cached --name-only --diff-filter=ACMR | sed 's| |\\ |g') --write --ignore-unknown
git update-index --again

# Run tests before committing
npm test

# Build, to catch a broken build before it is pushed. Nothing to stage: the
# output is gitignored and produced again at release time.
npm run build
